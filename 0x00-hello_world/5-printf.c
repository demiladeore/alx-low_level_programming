#include <stdio.h>

/**
* main - function prints a string
* Description - prints a string with printf
* Return: Always 0 (Success)
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art, ");
	return (0);
}
